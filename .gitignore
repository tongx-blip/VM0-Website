# local review artifacts
shots/
tmp-live/
*.log

# tooling byproducts
__pycache__/
*.pyc

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
node_modules/

# design exploration, not part of the site
redesign/
generated/

# the supplied font originals — only the built woff2 ships
src/fonts/*
!src/fonts/README.md

# the header review build — a full copy of site/, rebuilt by
# tools/build-header-options.py whenever it is needed
options-header/

# component SVGs fetched from the Avatar Composer — 300+ files of somebody
# else's build output. The composed avatars themselves ARE committed.
tools/avatar-cache/
